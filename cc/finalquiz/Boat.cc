#include "Boat.h"

namespace JeremysCode {
    /*-========================================================================/
    Boat()
    /========================================================================-*/
    Boat::Boat(void) {
        hits = 0;
    }

    /*-========================================================================/
    int getHits(void) const
    /========================================================================-*/
    int Boat::getHits(void) const {
        return hits;
    }

    /*-========================================================================/
    void setHits(int)
    /========================================================================-*/
    void Boat::setHits(int newHits) {
        hits = newHits;
    }
}
