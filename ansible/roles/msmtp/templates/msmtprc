# A system-wide configuration file for msmtp.
#
# {{ ansible_managed }}
#
# The purpose of this configuration file is to provide a mechanism by which
# services such as cron may send emails. For a suer to send email, they must
# have read access to this configuration file, and they must have the given
# email account's password. The password may be specified by several means,
# including as plain text in /etc/msmtprc or ~/.msmtprc, via secret-tool, or via
# gnome-keyring.
#
# For more, see: https://wiki.archlinux.org/index.php/Msmtp

# Define default values for all following accounts.
defaults
tls on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile ~/.msmtp.log

# Define a single account.
account   ichi-machines
host      smtp.gmail.com
port      587
from      ichi.machines@gmail.com
auth      on
user      ichi.machines
password  {{ msmtprc_password }}

# Define the default account.
account default: ichi-machines
