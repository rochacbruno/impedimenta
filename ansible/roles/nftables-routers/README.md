nftables-routers
================

Install and configure nftables.

This role should only be applied to a router.

Example Playbook
----------------

```yaml
- hosts: routers
  roles:
    - nftables
```
