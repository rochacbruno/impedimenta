1. An actual argument can be evaluated as an expression and is placed in the parentheses of a function call.
A formal parameter simply declares variables to be used, and their type. Formal parameters are declared in the parentheses of a function definition and its corresponding function prototype.
2.	a. void donut(int n_zero);
	b. int gear(int a, int b);
	c. void stuff_it(double x, double * y);
3.	a. char n_to_char(int n);
	b. int digits(double a, int b);
	c. int random(void);
4.
int sum(int a, int b)
{
	return a + b;
}
5.
double sum(double a, double b)
{
	return a + b;
}
6. void alter(int * a, int * b)
{
	int sum;
	int diff;

	sum = a + b;
	diff = a - b;
	a = sum;
	b = diff;
}
7.
void salami(int num)
{
	int count;

	for(count = 1; count <= num; num++)
		printf(" O salami mio!\n");
}
8.
int largest(int a, int b, int c)
{
	if(a > b && a > c)
		return a;
	else if(b > c)
		return b;
	else
		return c;
}
9.
Not today.
