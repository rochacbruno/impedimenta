The software provided by this installer module isn't particularly useful. It's
intended to act as a reference for myself, so I can remember how these darn
things are put together.

To create a source distribution for this module (e.g. tar.gz), run the
following::

    python setup.py sdist

To install the modules in this package, run the following::

    python setup.py install
